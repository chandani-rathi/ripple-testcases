import { track } from "ripple"
export component Button(props){
  const rest = track(() => {
    let { children, ...rest} = props;
    return rest;
  });
  
  <button {...@rest} >
      if(props.children) {
        <props.children />
      }
  </button>
}